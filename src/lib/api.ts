import { apiService } from './api-service'

// Export the enhanced API service instance for backward compatibility
export const api = apiService

// Export default for easy importing
export default apiService
